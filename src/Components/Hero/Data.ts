export interface Heros {
  heros: HeroElement[];
}

export interface HeroElement {
  artist: string;
  title: string;
  subHeading: string;
  subHeadingCredit: string;
  credit: string;
  paragraph: string;
  img: string;
}
